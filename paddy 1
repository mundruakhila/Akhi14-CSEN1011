#include <stdio.h>
int main()
{
    int a[50],i,j,c=0,n,k;
    scanf("%d",&n);
    for(k=0;k<n;k++)
        scanf("%d",&a[k]);
    for(i=0;i<n-1;i++)
        for(j=i+1;j<n;j++)
            if(a[i]==a[j])
                c++;
    printf("%d",c);
}
